fn is_even(int n) -> bool {
    return n % 2 == 0
}

fn is_positive(int n) -> bool {
    return n > 0
}

fn main() {
    int n = 4
    if (is_even(n) and is_positive(n)) {
        print("yes")
    }
}